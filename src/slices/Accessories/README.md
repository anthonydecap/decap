# Accessories slice

Displays a grid of accessories from **Prismic repeater items** only.

Each item: `custom_title`, `custom_price`, `currency`, `custom_image`, `custom_description`.

No cart or external product database.
